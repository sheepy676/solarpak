#ifndef CONFIG_H
#define CONFIG_H

#define PROGRAM_NAME "${PROJECT_NAME}"
#define PROGRAM_VERSION "${PROJECT_VERSION}"
#define PROGRAM_DESC "${PROJECT_DESCRIPTION}"
#define GIT_COMMIT "${GIT_COMMIT_HASH}"

#endif